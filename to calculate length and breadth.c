to calculate length and breadth

#include <stdio.h>

int main() {
     float len=5;
     float bredth=3;
     float area=(float)len*bredth;
     printf("the length is:%f\n",len);
     printf("the bredth is:%f\n",bredth);
     printf("the area is %f\n",area);
     
     return 0;
}